#include<iostream>
using namespace std;
int factorial(int n)
{
	if(n==0||n==1)
	return 1;
	else
	return n*factorial(n-1);
}
int main()
{
	int n,r;
	cout<<"Enter a Number:";
	cin>>n;
	r=factorial(n);
	cout<<"Factorial of "<<n<< "is:" << r;
	return 0;
}
